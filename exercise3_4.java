class exercise3_4
{
public static void main(String[] args) {
    int num= 456;// 변수 num을 선언후 456으로 초기화
    System.out.println(num/100*100);/* int는 소수점은 표시가안되기때문에 
    num의 백의 자리만 표시하게 할려면 100으로 나누에 4.***로 만든다음 (int기때문에 소수점이 날라가고)100을 다시곱해 400을 만든다.*/
}
}//변수 num의 값중에서 백의 자리이하는 버리는 코드이다.